# MurOS runtime directories (Debian)
# Volt compiled-template cache and PHP temp directory must exist and be
# writable by the web user, otherwise template rendering fails.
d /var/lib/php/cache 0775 www-data www-data -
d /var/lib/php/tmp 0775 www-data www-data -
